Index: R-4.2.2/src/main/errors.c
===================================================================
--- R-4.2.2.orig/src/main/errors.c
+++ R-4.2.2/src/main/errors.c
@@ -1080,7 +1080,7 @@ static void jump_to_top_ex(Rboolean traceback,
 	}
     }
 
-    R_jumpctxt(R_ToplevelContext, 0, NULL);
+    R_jumpctxt(R_ToplevelContext, 1, NULL);
 }
 
 void NORET jump_to_toplevel(void)
@@ -1534,7 +1534,7 @@ void NORET R_JumpToToplevel(Rboolean restart)
     if (c != R_ToplevelContext)
 	warning(_("top level inconsistency?"));
 
-    R_jumpctxt(R_ToplevelContext, CTXT_TOPLEVEL, NULL);
+    R_jumpctxt(R_ToplevelContext, 1, NULL);
 }
 #endif
 
